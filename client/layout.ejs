<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Joaquim d'Souza dot com<%= (route === '/') ? '' : (' | ' + title) %></title>
    <meta name="description" content="Joaquim d'Souza's web presence">
    <meta name="author" content="Joaquim d'Souza">

    <link rel="stylesheet" href="/global.css?v=<%= version %>">
    <link rel="stylesheet" href="index.css?v=<%= version %>">
    <script src="/loading.js?v=<%= version %>"></script>
</head>

<body>

<div id="container" class="container">

    <header>
        <div class="header__title-row">
            <h1><%= title %></h1>
            <%- include('./components/accessibility/control.ejs') %>
        </div>
        <% if (route !== '/') { %>
        <a href="/">Home</a>
        <% } %>
    </header>
    <main>
        <%- page %>
    </main>
    <footer>
        <small>
            <em>
                Site built at <%= date.toLocaleTimeString('en-GB'); %> on <%= date.toLocaleDateString('en-GB'); %>.
            </em>
        </small>
    </footer>

</div>

<script src="/global.js?v=<%= version %>"></script>
<script src="index.js?v=<%= version %>"></script>
<script src="/ready.js?v=<%= version %>"></script>
</body>
</html>